% ============================================================================
% PART 08 - FUNCTIONAL CLASSIFICATION
% ============================================================================

\labelsec{functional_classification}

This chapter demonstrates that the three SHT units encode sufficient information to classify DNA functional state with high accuracy.

\section{Classification Hypothesis}

\begin{keyresult}
\textbf{Hypothesis:} The geometric signature $[v_0,\,\eta_r,\,\Omega]$ distinguishes DNA functional class without any sequence information.
\end{keyresult}

This is non-trivial: conventional descriptors like GC content or length differ between classes but are not geometric. SHT bypasses sequence and directly characterizes 3D structure.

\section{Training Dataset}

\textbf{Two distinct labelling schemes} are used in this chapter and must be distinguished carefully:

\paragraph{Scheme A --- Manual functional labels ($N = 33$).}
Thirty-three structures were assigned to four functional categories (Table~\ref{tab:class_labels}) by inspecting PDB annotation records \emph{before} examining geometric values. This set validates geometric intuition and supports the blind-prediction case study (\S\ref{sec:blindpred}), but its size ($N = 33$) is too small for reliable cross-validated classifier training.

\paragraph{Scheme B --- Topology labels from PDB metadata ($N = 189$).}
The Random Forest classifier is trained and evaluated on the larger set of $N = 189$ structures whose structural topology (Canonical-Duplex, Protein-Comp/Wrapped, Strained-Helix) was determined from PDB metadata fields independently of geometric measurement. These three classes are coarser than Scheme A but provide a sufficient sample for stratified cross-validation.

\begin{table}[h]
    \centering
    \caption{Manually Labelled Structures for Geometric Validation ($N=33$, Scheme A)}\labeltab{class_labels}
    \begin{tabular}{llll}
        \toprule
        \textbf{Class} & $N$ & \textbf{Structures} & \textbf{Known signature} \\
        \midrule
        Nucleosome      & 2  & 1AOI, 1F66           & High Vij, high Mamton \\
        Protein-bound   & 11 & Various TF complexes  & Moderate Vij, variable Mamton \\
        Free duplex     & 16 & Canonical B-DNA       & Low Vij, low Mamton \\
        Non-canonical   & 4  & G4, damage, junction  & Extreme Shambhian \\
        \bottomrule
        \multicolumn{2}{l}{\textbf{Total}} & 33 & \\
        \bottomrule
    \end{tabular}
\end{table}

Labels were assigned from PDB metadata \textit{before} examining geometric values. The remaining 167 structures are unlabeled and serve as prediction targets.

\section{Model Architecture}

A Random Forest Classifier was chosen for:
\begin{itemize}
    \item Robustness to scale differences between units (no normalization needed)
    \item Native feature importance via Gini impurity
    \item Non-linear decision boundaries without manual kernel choice
    \item Calibrated probability outputs for confidence scoring
\end{itemize}

\textbf{Hyperparameters:} 300 trees, OOB scoring enabled, Gini criterion (scikit-learn defaults). Evaluated on $N = 189$ structures across three classes (Canonical-Duplex, Protein-Comp/Wrapped, Strained-Helix).

\section{Performance Results}

\subsection{Cross-Validation}

\begin{keyresult}
    \textbf{5-fold stratified CV accuracy:} $\mathbf{81.0\%}$ \quad
    \textbf{OOB accuracy:} $\mathbf{78.8\%}$ \quad
    \textbf{ROC AUC (macro OvR):} $\mathbf{0.807}$

    Trained and tested on $N = 189$ topology-labelled structures (Scheme B, 3 classes). Chance performance = 33\%.
\end{keyresult}

The OOB score ($78.8\%$) serves as an independent held-out estimate, closely matching the CV score and confirming that the CV result is not inflated by data leakage. These results are considered \textbf{exploratory}: the topology labels are coarse, the dataset is crystallographic and non-random, and independent replication on an orthogonal dataset is needed before production use.

\subsection{Per-Class Performance (Measured by Stratified CV)}

\begin{table}[h]
    \centering
    \caption{Per-Class Precision, Recall, and F1 (5-fold CV, $N=189$)}\labeltab{per_class}
    \begin{tabular}{llllll}
        \toprule
        \textbf{Class} & $N$ & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Comments} \\
        \midrule
        Canonical-Duplex     & 136 & 0.83 & 0.95 & 0.89 & Largest class; cleanest boundary \\
        Protein-Comp/Wrapped &  25 & 0.79 & 0.60 & 0.68 & Some overlap with canonical \\
        Strained-Helix       &  28 & 0.60 & 0.32 & 0.42 & Most heterogeneous; hardest to classify \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Feature Importance}

The trained forest assigns the following Gini-importance to each feature:

\begin{table}[h]
    \centering
    \caption{SHT Feature Importance (Random Forest)}\labeltab{feature_importance}
    \begin{tabular}{lll}
        \toprule
        \textbf{Feature} & \textbf{Importance} & \textbf{Interpretation} \\
        \midrule
        Mamton ($\Omega$) & \textbf{40.5\%} & Most discriminative; extreme range drives class separation \\
        Vij ($v_0$)       & 36.3\%          & Second; separates bent from straight DNA \\
        Shambhian ($\eta_r$) & 23.3\%       & Supports Strained-Helix class discrimination \\
        \bottomrule
    \end{tabular}
\end{table}

Mamton is the most important feature despite being a derived quantity (3rd-order moment). Its dominance reflects the extreme dynamic range: $\Omega$ spans 3.7 orders of magnitude across the dataset, providing strong class separation.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.65\textwidth]{feature_importance}
    \caption{Random Forest feature importance (Gini impurity). Mamton is the most discriminative unit (41\%), followed by Shambhian (33\%) and Vij (26\%).}\labelfig{feature_importance}
\end{figure}

\section{Blind Predictions}

\subsection{Case Study: Structure 1F66}

Before looking up PDB metadata, 1F66 was presented to the classifier as unlabeled. The feature vector was $[v_0 = 0.297,\;\eta_r = 0.828,\;\Omega = 26{,}441]$.

\begin{keyresult}
    \textbf{Prediction:} Nucleosome (probability $> 0.99$) \\
    \textbf{Confirmation:} H2A.Z-containing nucleosome core particle \citep{Suto2000} \\
    \textbf{Verdict:} Correct --- blind prediction confirmed by PDB record
\end{keyresult}

The z-scores of 1F66 make this prediction unsurprising in retrospect: both Vij and Mamton are $+4$--$+5\sigma$ above the dataset mean. But crucially, this structure was \textbf{not in the training set} and was classified on geometry alone.

\subsection{Predictions on Unlabeled Structures}

The remaining 167 unlabeled structures were classified. Key predictions:

\begin{itemize}
    \item \textbf{Free duplex} (predicted): 89 structures --- standard B-DNA, consistent with crystallography conditions
    \item \textbf{Protein-bound} (predicted): 58 structures --- various complexes, likely overfit slightly given small training set
    \item \textbf{Non-canonical} (predicted): 18 structures --- geometric anomalies worth experimental follow-up
    \item \textbf{Nucleosome candidate} (predicted, extreme $\Omega$): 2 structures besides 1AOI/1F66 --- merit curated PDB annotation check
\end{itemize}

These predictions are exploratory. Experimental verification and larger labeled datasets are needed before production use.

\section{Decision Boundaries and Geometric Interpretation}

The classifier's first branching level (root node of most trees) uses a Mamton threshold at $\Omega \approx 5{,}000$: structures above this are almost exclusively protein-wrapped or nucleosomal. The second level uses Vij to distinguish protein-bound from nucleosomal. Shambhian is most active in the final level to separate non-canonical from free-duplex.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{functional_classification_space}
    \caption{Functional classification in SHT geometric space. Points are colored by predicted class. Nucleosome structures (1AOI, 1F66) are visually isolated in the extreme Vij--Mamton corner. Free duplex structures cluster at low Mamton. The PCA projection collapses three dimensions to two for visualization.}\labelfig{functional_class_space}
\end{figure}

This three-level hierarchy maps naturally onto biological intuition: \textit{how rough is it?} $\rightarrow$ \textit{how bent is it?} $\rightarrow$ \textit{how distorted is the cross-section?}

